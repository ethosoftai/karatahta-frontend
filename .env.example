VITE_API_BASE_URL=https://karatahta-backend-production.up.railway.app

# Comma-separated emails that can see the developer portal (backend mode
# selector, health checks) and the per-lesson developer console (job
# metrics, raw log output) in the sidebar/studio. Everyone else never sees
# them. Leave empty to hide from everyone.
VITE_ADMIN_EMAILS=
